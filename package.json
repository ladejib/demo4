{
  "name": "playwright-k8s-template",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "merge-reports": "npx playwright merge-reports --reporter html ./reports"
  },
  "dependencies": {
    "@playwright/test": "^1.50.0"
    "playwright": "^1.52.0"
  }
}
